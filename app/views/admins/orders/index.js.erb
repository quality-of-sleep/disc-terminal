$("<%= escape_javascript(render partial:'index') %>").appendTo('#orders');
$('#next').html('<%= escape_javascript(link_to_next_page @orders, "次を表示", remote: true) %>');

// $("#orders").html("<%= escape_javascript(render partial:'index') %>");
// $('#paginator').html('<%= escape_javascript(paginate @orders, remote: true, class: "paginate") %>');
